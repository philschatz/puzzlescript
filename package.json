{
  "scripts": {
    "lint": "standard --fix index.js src/**/*.js test/**/*.js",
    "tsc": "tsc src/*.ts --target ES2015 --lib ES2015 --module commonjs",
    "start": "node index.js",
    "test": "jest --coverage --updateSnapshot",
    "test-debug": "node --inspect-brk $(npm bin)/jest -i --updateSnapshot $@",
    "posttest": "npm run-script lint",
    "watch": "jest --updateSnapshot --watch"
  },
  "dependencies": {
    "@types/lodash": "^4.14.108",
    "axel": "0.0.3",
    "eventemitter2": "^5.0.1",
    "glob": "^7.1.2",
    "jest": "^22.4.3",
    "lodash": "^4.17.10",
    "ohm-js": "^0.14.0",
    "pify": "^3.0.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "standard": "^11.0.1",
    "typescript": "^2.8.3"
  }
}
