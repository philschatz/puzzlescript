{
  "scripts": {
    "lint": "standard --fix index.js src/**/*.js test/**/*.js",
    "start": "node index.js",
    "poststart": "npm run-script lint",
    "test": "jest --updateSnapshot",
    "test-debug": "node --inspect-brk $(npm bin)/jest -i --updateSnapshot $@",
    "watch": "jest --updateSnapshot --watch"
  },
  "dependencies": {
    "axel": "0.0.3",
    "glob": "^7.1.2",
    "jest": "^22.4.3",
    "ohm-js": "^0.14.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "standard": "^11.0.1"
  }
}
